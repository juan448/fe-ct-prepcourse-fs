const listarPropiedades = (objeto) => {
    // Lista y retorna todas las propiedades del objeto
    return Object.keys(objeto); // Retorna un arreglo con las propiedades
  };
  
  module.exports = listarPropiedades;
  