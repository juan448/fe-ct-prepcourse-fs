// Requiere el archivo que contiene la variable
const miBoolean = require('./miBoolean');

// Imprime el valor de la variable
console.log(miBoolean); // Debería imprimir: true (o false, dependiendo del valor que hayas asignado)
