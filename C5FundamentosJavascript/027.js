function obtenerAreaRectangulo(alto, ancho) {
    // Retorna el área del rectángulo multiplicando el alto por el ancho
    return alto * ancho;
  }
  
  module.exports = obtenerAreaRectangulo;
  